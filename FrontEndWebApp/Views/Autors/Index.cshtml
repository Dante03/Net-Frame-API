@model IEnumerable<FrontEndWebApp.Models.Autor>

@{
    ViewBag.Title = "Autores";
}

<h2>Listado</h2>

<p>
    @Html.ActionLink("Agregar Autor", "Create")
</p>
<table class="table">
    <tr>
        @*<th>*@
        @*@Html.DisplayNameFor(model => model.Id_Autor)*@
        @*</th>*@
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nacionalidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Edad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <!--<td>
            @Html.HiddenFor(modelItem => item.Id_Autor)-->
        @*</td>*@
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nacionalidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Edad)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Id_Autor }) |
            @Html.ActionLink("Detalles", "Details", new { id = item.Id_Autor }) |
            @Html.ActionLink("Borrar", "Delete", new { id = item.Id_Autor })
        </td>
    </tr>
}

</table>
